import { RoleTabScreen } from "@/components/role-tab-screen";

export default function Screen() {
  return (
    <RoleTabScreen
      summary="Look up a resident. Full management stays on the web."
      title="Residents"
    />
  );
}
