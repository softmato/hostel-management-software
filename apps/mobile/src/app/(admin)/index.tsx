import { RoleTabScreen } from "@/components/role-tab-screen";

export default function Screen() {
  return (
    <RoleTabScreen
      summary="Occupancy, dues and today’s activity — read-only."
      title="Overview"
    />
  );
}
