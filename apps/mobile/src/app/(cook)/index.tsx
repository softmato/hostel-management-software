import { RoleTabScreen } from "@/components/role-tab-screen";

export default function Screen() {
  return (
    <RoleTabScreen
      summary="Today’s menu, resident count and the Food Ready buttons."
      title="Today"
    />
  );
}
