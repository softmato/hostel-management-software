import { RoleTabScreen } from "@/components/role-tab-screen";

export default function Screen() {
  return (
    <RoleTabScreen
      summary="Notices from your hostel, filtered by category."
      title="Notices"
    />
  );
}
