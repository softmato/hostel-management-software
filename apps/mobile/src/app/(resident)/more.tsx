import { RoleTabScreen } from "@/components/role-tab-screen";

export default function Screen() {
  return (
    <RoleTabScreen
      summary="Profile, complaints, night status, referrals and settings."
      title="More"
    />
  );
}
