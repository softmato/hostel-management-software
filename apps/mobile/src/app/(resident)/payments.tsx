import { RoleTabScreen } from "@/components/role-tab-screen";

export default function Screen() {
  return (
    <RoleTabScreen
      summary="Invoices, payment claims, receipts and gateway checkout."
      title="Payments"
    />
  );
}
