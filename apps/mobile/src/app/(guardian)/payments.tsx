import { RoleTabScreen } from "@/components/role-tab-screen";

export default function Screen() {
  return (
    <RoleTabScreen
      summary="Rent status for your ward, read-only."
      title="Payments"
    />
  );
}
